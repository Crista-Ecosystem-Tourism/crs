# CLAUDE.md — монорепа `crs`

Перед изменениями кода сервисов прочитай **`AGENTS.md`**: правки вносятся в **канонические** репозитории (`frontend`, `ai_agent`, …), не в копии под `crs/<сервис>/`; затем push и при необходимости `git pull` в `crs`.

Инфраструктура Compose / Docker / otel — правь здесь, в **`crs`**, и пушь в этот репозиторий.
